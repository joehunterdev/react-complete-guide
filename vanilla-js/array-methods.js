//Array methods
const numbers = [1,2,3]

//map takes arrow
const doubleNumArray = numbers.map((num) => {

    //lots stuff in docs for arrow funcitons
    //Apply this to each element of the array
    return num * 2
});

console.log(numbers)

console.log(doubleNumArray)

// Excercise
function transferToObjects(...numbers){
 
    return numberObject.object()
    
}

const transferToObjects2 = numbers.map((newObj) => {    return v = {newObj}; }); 

console.log(transferToObjects2)


const transformToObjects  = numbers.map((newObj) => {    return v = {newObj}; }); 
console.log(transformToObjects)


const numbers = [1,2,3]
const transformToObjects  = numbers.map((vval) => {    return v = {val}; }); 
console.log(transformToObjects)